#!/bin/bash -i

rm -f stdin stdout stderr
mkfifo stdin
touch stdout stderr

exec 0<>stdin
exec 1<>stdout
exec 2<>stderr

nohup bash -i -s <&0 >&1 2>&2 & 

#bash -i -s <&10 >&11 2>&12 &
#echo $! > .bash.pid
#echo "info: bash launched. (pid: $(cat .bash.pid))"
#
#trap 'kill -9 $(cat .bash.pid); rm .bash.pid; echo "info: exit."' SIGINT
#sleep inf &
#wait $!

#rm -f stdin stdout stderr
